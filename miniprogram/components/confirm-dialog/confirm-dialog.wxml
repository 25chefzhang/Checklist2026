<view class="dialog-container" wx:if="{{visible}}">
  <!-- 遮罩层 -->
  <view class="mask" bind:tap="onMaskTap" catch:touchmove="noop"></view>

  <!-- 弹窗卡片 -->
  <view class="dialog-card" catch:tap="noop">
    <!-- 标题 -->
    <view class="dialog-title">{{title}}</view>

    <!-- 内容 -->
    <view class="dialog-content" wx:if="{{content}}">{{content}}</view>

    <!-- 按钮区 -->
    <view class="dialog-actions">
      <view
        wx:if="{{showCancel}}"
        class="dialog-btn cancel-btn"
        bind:tap="onCancel"
      >{{cancelText}}</view>
      <view
        class="dialog-btn confirm-btn {{showCancel ? '' : 'full-width'}}"
        bind:tap="onConfirm"
      >{{confirmText}}</view>
    </view>
  </view>
</view>
